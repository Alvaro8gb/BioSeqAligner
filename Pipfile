[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
app = "streamlit run app.py"
format = "black app ./"

[packages]
streamlit = ">=1.28.0"
numpy = ">=1.24.0"

[dev-packages]
ipykernel = "*"

[requires]
python_version = "3.12"
python_full_version = "3.12.3"
